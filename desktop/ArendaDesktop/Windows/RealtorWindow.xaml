<Window x:Class="ArendaDesktop.Windows.RealtorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Недвижимость.РФ — Мои объявления"
        Width="1100" Height="750"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Шапка (белая как в Figma) -->
        <Border Grid.Row="0" Background="White" Padding="24,14"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Недвижимость.РФ" FontSize="18" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="UserEmailText" FontSize="13"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center" Margin="0,0,16,0" />
                    <Button Content="Каталог" Style="{StaticResource SecondaryButton}"
                            Padding="16,8" FontSize="13" Margin="0,0,8,0"
                            Click="CatalogButton_Click" />
                    <Button Content="Выйти" Style="{StaticResource PrimaryButton}"
                            Padding="20,8"
                            Click="LogoutButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Заголовок и кнопка добавления -->
        <Border Grid.Row="1" Margin="24,20,24,0">
            <Grid>
                <TextBlock Text="Мои объявления" FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center" />
                <Button Content="+ Добавить объявление"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Right" Padding="20,10"
                        FontSize="14" Click="AddProperty_Click" />
            </Grid>
        </Border>

        <!-- Сообщение -->
        <Border x:Name="MessageBorder" Grid.Row="2" Margin="24,12,24,0"
                CornerRadius="8" Padding="12,8" Visibility="Collapsed"
                VerticalAlignment="Top">
            <TextBlock x:Name="MessageText" FontSize="13" />
        </Border>

        <!-- Список объявлений -->
        <ScrollViewer Grid.Row="2" Margin="24,16,24,24" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="PropertiesList" />
        </ScrollViewer>

        <!-- Загрузка -->
        <TextBlock x:Name="LoadingText" Grid.Row="2" Text="Загрузка..."
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="16" Foreground="{StaticResource TextSecondaryBrush}"
                   Visibility="Collapsed" />
    </Grid>
</Window>
